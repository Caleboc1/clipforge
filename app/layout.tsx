import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'ClipForge – Turn YouTube Videos into Viral Clips',
  description: 'AI-powered tool that extracts the most engaging moments from long YouTube videos and turns them into viral short-form clips for TikTok and YouTube Shorts.'
}

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en" className="dark">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
